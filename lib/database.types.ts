// Placeholder — replaced with the full generated schema type in step 2.
// After linking your Supabase project you can regenerate this with:
//   npx supabase gen types typescript --linked > lib/database.types.ts
export type Database = any;
